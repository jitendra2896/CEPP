#ifndef HELLO
#define HELLO


#include<stdio.h>
#include<string.h>
#define max 100
int isLegal(char[]);
int convertToInteger(char[], int[]);
void check(int[], int, int);
int count_lines(FILE*);

#endif
